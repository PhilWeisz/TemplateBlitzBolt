export interface TypographyConfig {
  fontFamily: string;
  baseFontSize: number;
  headingRatio: number;
  lineHeight: number;
  paragraphSpacing: number;
  fontWeights: {
    normal: number;
    medium: number;
    bold: number;
  };
  letterSpacing: number;
}